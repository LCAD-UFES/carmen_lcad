#############################################################################
# Makefile for building: bin/mpu9250-OpenGl
# Generated by qmake (3.0) (Qt 5.5.1)
# Project:  mpu9250-opengl.pro
# Template: app
# Command: /usr/lib/x86_64-linux-gnu/qt5/bin/qmake -spec linux-g++-64 -o Makefile mpu9250-opengl.pro
#############################################################################


include ../Makefile.conf

MODULE_NAME = IMU VIEWER
MODULE_COMMENT = IMU Viewer Module

LINK          = g++

DEFINES       =
CFLAGS        =
CXXFLAGS      =

LFLAGS        =

IFLAGS =

ifneq ($(PROCESSOR),aarch64)
QMAKE         = /usr/lib/x86_64-linux-gnu/qt5/bin/qmake
else
QMAKE         = /usr/local/qt4.8.7/bin/qmake
endif

SOURCES       =

PUBLIC_INCLUDES = 

PUBLIC_LIBRARIES = 

PUBLIC_BINARIES = imu_viewer 

ifneq ($(PROCESSOR),aarch64)
TARGETS = imu_viewer
endif

# Este modulo eh compilado pelo qmake. Veja o arquivo mpu9250-opengl.pro
imu_viewer: *.cpp
	+$(QMAKE) -spec linux-g++-64 -o Makefile.qmake mpu9250-opengl.pro
	+make -f Makefile.qmake
	
clean: clean_imu_viewer

clean_imu_viewer:
	if [ -f Makefile.qmake ]; then make -f Makefile.qmake clean; rm -f Makefile.qmake; fi;


include ../Makefile.rules

