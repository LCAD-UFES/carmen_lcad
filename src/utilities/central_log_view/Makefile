include ../../Makefile.conf

MODULE_NAME = Central Log View
MODULE_COMMENT = Analyze log files generated by IPC central server 

LINK = g++
CFLAGS += -Wall
CXXFLAGS += -Wno-ignored-qualifiers

IFLAGS += 

LFLAGS += \
	-lparam_interface \
	-lipc \
	-lglobal

SOURCES = \
	central_log_view_main.cpp

PUBLIC_INCLUDES = 

PUBLIC_LIBRARIES = 

PUBLIC_BINARIES = \
	central_log_view

TARGETS = \
	central_log_view

central_log_view: central_log_view_main.o

include ../../Makefile.rules
