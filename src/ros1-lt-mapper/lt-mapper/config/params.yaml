ltslam:
  sessions_dir: "/root/catkin_ws/src/lt-mapper/sessions/in/" # end with /
  central_sess_name: "session1" # should be same as the directory name of that session
  query_sess_name: "session2" # should be same as the directory name of that session

  save_directory: "/root/catkin_ws/src/lt-mapper/sessions/out/session1-session2-manyloops/" # end with "/"
  is_display_debug_msgs: false
  loopFitnessScoreThreshold: 0.7
  kNumSCLoopsUpperBound: 1000 # if you use an enough number, use all the detected loops
  kNumRSLoopsUpperBound: 0 # if you use an enough number, use all the detected loops
  numberOfCores: 16 # currently hard-coded in the utility.cpp